@model BasementHoofersQuiz.Models.QuizModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>QuizModel</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.quizTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.quizTitle)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.quizDescription)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.quizDescription)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.quizId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
